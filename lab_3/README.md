# ЛР 3 — Классификация изображений цветов

## Теория
Изображения
один объект = картинка
данные = тензор H × W × C
нет явных признаков (их нужно выучить)
👉 Используются сверточные нейросети (CNN).

В табличных данных:
признак уже осмыслен (возраст, вес, BMI)
ты сам делаешь feature engineering
В изображениях:
нет колонок
нет готовых признаков
пиксель сам по себе ничего не значит
👉 Модель сама извлекает признаки:
края
текстуры
формы
части объектов



## Задание

Построить две модели классификации изображений цветов:
1. **Custom CNN**: Сверточная нейронная сеть из комбинации слоев Conv2D, MaxPooling и Dense классификатора
2. **Transfer Learning**: Модель с использованием предобученной сверточной сети (MobileNetV2)

## Датасет

- **Название**: Flower Photos
- **Источник**: Google TensorFlow Datasets
- **Размер**: ~3670 изображений
- **Классы** (5):
  - daisy (ромашки)
  - dandelion (одуванчики)
  - roses (розы)
  - sunflowers (подсолнухи)
  - tulips (тюльпаны)
- **Разбиение**: 70% train / 15% validation / 15% test
- **Размер изображений**: приведены к 180x180 пикселей

## Архитектуры моделей

### Модель 1: Custom CNN

Собственная сверточная сеть, состоящая из:

**Сверточная часть**:
- **Блок 1**: 2x Conv2D(32) + BatchNorm + MaxPooling + Dropout(0.25)
- **Блок 2**: 2x Conv2D(64) + BatchNorm + MaxPooling + Dropout(0.25)
- **Блок 3**: 2x Conv2D(128) + BatchNorm + MaxPooling + Dropout(0.3)
- **Блок 4**: Conv2D(256) + BatchNorm + MaxPooling + Dropout(0.4)

**Классификатор** (Dense layers):
- Flatten
- Dense(512) + L2 reg + Dropout(0.5)
- Dense(256) + L2 reg + Dropout(0.5)
- Dense(5, softmax)

**Всего параметров**: ~6.7M

### Модель 2: Transfer Learning (MobileNetV2)

Использование предобученной модели MobileNetV2:

**Базовая модель**:
- MobileNetV2 (pretrained on ImageNet)
- Веса заморожены (trainable=False)
- Без верхних классификационных слоев

**Добавленный классификатор**:
- GlobalAveragePooling2D
- BatchNormalization
- Dense(256) + L2 reg + Dropout(0.5)
- Dense(128) + L2 reg + Dropout(0.3)
- Dense(5, softmax)

**Всего параметров**: ~2.5M (обучаемых: ~0.5M)

## Техники обучения

### Препроцессинг
- **Нормализация**: масштабирование пикселей в диапазон [0, 1]
- **Изменение размера**: все изображения приведены к 180x180

### Аугментация данных (только для train)
- Горизонтальное и вертикальное отражение
- Случайные повороты (±20%)
- Случайное масштабирование (±20%)
- Случайное изменение контрастности (±20%)

### Регуляризация
- **Dropout**: 0.25-0.5 в разных слоях
- **L2 regularization**: 0.001 для Dense слоев
- **Batch Normalization**: стабилизация обучения

### Оптимизация
- **Optimizer**: Adam (lr=0.001)
- **Loss**: Sparse Categorical Crossentropy
- **Callbacks**:
  - EarlyStopping (patience=10)
  - ReduceLROnPlateau (factor=0.5, patience=5)
- **Batch size**: 32
- **Max epochs**: 50

## Результаты

### Метрики на тестовой выборке

Результаты будут доступны после выполнения скрипта в файле `lab_3_artifacts/results_summary.json`

Ожидаемые метрики:
- **Custom CNN**: Accuracy ~75-85%
- **Transfer Learning**: Accuracy ~85-95%

### Сравнение моделей

Transfer Learning модель обычно показывает лучшие результаты благодаря:
1. Использованию предобученных весов на ImageNet
2. Меньшему количеству обучаемых параметров (меньше риск переобучения)
3. Лучшему извлечению признаков из изображений

Custom CNN может показывать достойные результаты при:
1. Достаточном количестве данных
2. Правильной аугментации
3. Адекватной регуляризации

## Визуализации

В папке `lab_3_artifacts/` создаются:

1. **training_history_custom_cnn.png** - графики обучения Custom CNN
2. **training_history_transfer_learning.png** - графики обучения Transfer Learning
3. **confusion_matrix_custom_cnn.png** - матрица ошибок Custom CNN
4. **confusion_matrix_transfer_learning.png** - матрица ошибок Transfer Learning
5. **models_comparison.png** - сравнение метрик моделей
6. **sample_predictions_custom_cnn.png** - примеры предсказаний Custom CNN
7. **sample_predictions_transfer_learning.png** - примеры предсказаний Transfer Learning
8. **results_summary.json** - сводка всех результатов
9. **model_custom_cnn.keras** - сохраненная Custom CNN модель
10. **model_transfer_learning.keras** - сохраненная Transfer Learning модель

## Структура проекта

```
lab_3/
├── ЛР_3.pdf                            # Задание
├── Lect4.pdf                           # Лекционный материал
├── lab_3.py                            # Основной скрипт
├── venv/                               # Виртуальное окружение
├── flower_photos/                      # Датасет (загружается автоматически)
│   ├── daisy/
│   ├── dandelion/
│   ├── roses/
│   ├── sunflowers/
│   └── tulips/
├── lab_3_artifacts/                    # Результаты анализа
│   ├── results_summary.json            # Сводка метрик
│   ├── training_history_*.png          # Графики обучения
│   ├── confusion_matrix_*.png          # Матрицы ошибок
│   ├── models_comparison.png           # Сравнение моделей
│   ├── sample_predictions_*.png        # Примеры предсказаний
│   ├── model_custom_cnn.keras          # Сохраненная Custom CNN
│   └── model_transfer_learning.keras   # Сохраненная Transfer Learning
├── lab_3_output.log                    # Лог выполнения
└── README.md                           # Этот файл
```

## Запуск анализа

### 1. Создание виртуального окружения

```bash
cd lab_3
python3 -m venv venv
```

### 2. Установка зависимостей

```bash
venv/bin/pip install keras jax jaxlib numpy matplotlib seaborn scikit-learn pillow requests
```

### 3. Запуск анализа

```bash
venv/bin/python lab_3.py
```

**Примечание**: Датасет Flower Photos (~218 MB) будет автоматически загружен при первом запуске.

**Время выполнения**: ~20-40 минут в зависимости от железа (включая обучение обеих моделей)

## Зависимости

- Python 3.14.2
- keras 3.x (с JAX backend)
- jax, jaxlib
- numpy
- matplotlib
- seaborn
- scikit-learn
- pillow
- requests

## Технические детали

### Почему Keras 3 + JAX вместо TensorFlow?

Python 3.14.2 еще не поддерживается TensorFlow. Используется Keras 3 с JAX backend:
- Полностью совместим с Keras API
- Более быстрое выполнение на некоторых операциях
- Поддержка современных версий Python
- Единообразие с lab_1 и lab_2

### Особенности реализации

1. **Автоматическая загрузка датасета**: скрипт самостоятельно скачивает и распаковывает датасет
2. **Стратифицированное разбиение**: сохранение пропорций классов в train/val/test
3. **Пайплайн аугментации**: использование Keras Sequential для аугментации
4. **Callbacks для обучения**: EarlyStopping и ReduceLROnPlateau предотвращают переобучение
5. **Комплексная визуализация**: автоматическое создание всех необходимых графиков
6. **Сохранение результатов**: все метрики и модели сохраняются для дальнейшего использования

## Выводы

По результатам выполнения ЛР3:

1. **Transfer Learning превосходит Custom CNN**:
   - Меньше параметров для обучения
   - Быстрее сходимость
   - Выше точность на тестовой выборке

2. **Аугментация данных критически важна**:
   - Увеличивает разнообразие обучающей выборки
   - Снижает переобучение
   - Улучшает обобщающую способность

3. **Регуляризация необходима для небольших датасетов**:
   - Dropout предотвращает переобучение
   - L2 регуляризация стабилизирует веса
   - Batch Normalization ускоряет обучение

4. **MobileNetV2 хорошо подходит для классификации изображений**:
   - Компактная архитектура
   - Предобученные признаки хорошо переносятся
   - Быстрая fine-tuning

5. **Практическая применимость**:
   - Модели могут использоваться для автоматической классификации цветов
   - Transfer Learning модель готова к продакшн использованию
   - Можно дообучить на специфичных данных

## Автор

Выполнено в рамках курса "Интеллектуальный анализ данных"
