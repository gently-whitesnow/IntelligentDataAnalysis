## 1. Главное отличие: тип данных

### Лаба 1 и 2

**Табличные данные**

* каждая строка = объект
* каждый столбец = признак
* модель видит **вектор чисел**

Примеры:

* бетон: `cement, water, age, ...`
* ожирение: `Age, Height, BMI, Gender_encoded, ...`

👉 Используются **Dense / GRU** сети, признаки подаются напрямую.

---

### Лаба 3

**Изображения**

* один объект = **картинка**
* данные = тензор `H × W × C`
* нет явных признаков (их нужно *выучить*)

👉 Используются **сверточные нейросети (CNN)**. 

---

## 2. Почему нельзя “как раньше”

В табличных данных:

* признак уже осмыслен (возраст, вес, BMI)
* ты **сам делаешь feature engineering**

В изображениях:

* нет колонок
* нет готовых признаков
* пиксель сам по себе **ничего не значит**

👉 Модель **сама извлекает признаки**:

* края
* текстуры
* формы
* части объектов

---

## 3. Архитектурное отличие (ключевое)


### В прошлых лабах

```text
Input → Dense → Dense → Output
```

* каждый нейрон видит **всё сразу**
* нет понятия пространства

---

### В этой лабе

```text
Image → Conv → Conv → Pool → Conv → … → Dense → Output
```

* нейроны видят **локальные области**
* учитывается **пространственная структура**
* параметры **разделяются** (weight sharing)

---

## 4. Новый этап, которого раньше не было

### 🔹 Аугментация данных

```python
RandomFlip
RandomRotation
RandomZoom
RandomContrast
```

Зачем:

* данных мало
* изображения легко переобучаются
* аугментация = “искусственно больше данных”

👉 В табличных данных такого **не существует**.

---

## 5. Transfer Learning — главное новшество

### Что было раньше

* модель обучалась **с нуля**
* все веса случайные

---

### Что здесь добавилось

```python
MobileNetV2(weights="imagenet")
```

Это значит:

* модель **уже умеет** распознавать:

  * края
  * формы
  * текстуры
* ты дообучаешь её **под цветы**

👉 Это принципиально другой подход:

> *не учим с нуля — используем чужой опыт*

---

## 6. Отличие в целевой переменной

| Лаба | Тип выхода          |
| ---- | ------------------- |
| 1    | число (регрессия)   |
| 2    | класс (таблица)     |
| 3    | класс (изображение) |

Но важно:

* в лабе 3 **класс выводится из визуальных паттернов**, а не из признаков

---

## 7. Различие пайплайнов целиком

### Лаба 1–2

```text
EDA
→ очистка
→ кодирование категорий
→ feature engineering
→ scaling
→ Dense / GRU
```

---

### Лаба 3

```text
Загрузка изображений
→ resize
→ нормализация
→ аугментация
→ CNN / Transfer Learning
→ визуальный анализ ошибок
```

---

## 8. Сильная формулировка для защиты

Если спросят *«чем эта работа отличается?»*, можно ответить так:

> В отличие от предыдущих лабораторных работ, в данной работе рассматривается задача классификации изображений, где признаки не заданы явно и извлекаются автоматически с помощью сверточных нейронных сетей. Дополнительно используется подход transfer learning с предобученной моделью MobileNetV2, что принципиально отличает метод обучения от моделей, обучаемых с нуля на табличных данных.

---

## 9. Суть в одном абзаце

> Предыдущие лабораторные были посвящены табличным данным, где признаки задаются явно и обрабатываются вручную. В этой лабораторной данные представлены изображениями, поэтому используется сверточная нейросеть, которая автоматически извлекает признаки, а также применяется transfer learning для повышения качества и скорости обучения.
